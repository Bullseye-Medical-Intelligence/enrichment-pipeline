# BEMI Pipeline — Operator Runbook

This guide covers everything you need to run the pipeline and review results.
No coding required after initial setup.

---

## 1. First-Time Setup (do this once per machine)

### Step 1: Get the code

Open a terminal (PowerShell on Windows, Terminal on Mac) and run:

```
cd C:\users\yourname\desktop\bemi
git clone https://github.com/Bullseye-Medical-Intelligence/enrichment-pipeline.git enrichment
cd enrichment
git checkout main
```

### Step 2: Install Python packages

```
cd pipeline-api
python -m pip install -r requirements.txt
```

### Step 3: Create your .env file

```
copy .env.example .env
```

Open `.env` in Notepad (or any text editor) and fill in these values:

```
PIPELINE_REPO_PATH=      ← full path to the enrichment folder (e.g. C:\users\rajiv\desktop\bemi\enrichment)
OUTPUT_RUNS_PATH=        ← where run output goes (e.g. C:\users\rajiv\desktop\bemi\output\runs)
UI_USERNAME=             ← your login name (e.g. rajiv)
UI_PASSWORD=             ← your login password (choose anything secure)
SESSION_SECRET_KEY=      ← a long random string (protects your session)
```

To generate the random string, run:
```
python -c "import secrets; print(secrets.token_urlsafe(32))"
```
Use it for `SESSION_SECRET_KEY` (which signs your login session). Login is
session-cookie based, using `UI_USERNAME` / `UI_PASSWORD` — there is no API key.

Also fill in your enrichment pipeline's `.env` in the parent folder:
```
ANTHROPIC_API_KEY=       ← your Anthropic API key
OPENAI_API_KEY=          ← your OpenAI API key
```

### Step 4: Create the output folder

```
mkdir C:\users\rajiv\desktop\bemi\output\runs
```
(adjust path to match what you set in OUTPUT_RUNS_PATH)

---

## 2. Starting the App (do this every time)

### Windows — double-click to launch

In the repo root folder you will find `start-bemi.bat`. Double-click it.

A terminal window opens, the server starts, and your browser opens automatically to the login page. Leave the terminal window open while you work. To stop the app, close the window or press Ctrl+C inside it.

**To pin it to your Desktop:** Right-click `start-bemi.bat` → "Create shortcut" → move the shortcut to your Desktop. Rename it "BEMI Pipeline".

### Mac / Linux — run once in Terminal

```bash
./start-bemi.sh
```

The server starts and your browser opens automatically. Press Ctrl+C to stop.

### Manual start (any platform)

If the launcher scripts don't work, you can always start manually:

```
cd pipeline-api
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

You should see:
```
Application startup complete.
Uvicorn running on http://0.0.0.0:8000
```

---

## 3. Logging In

1. Open your browser and go to: **http://localhost:8000/login**
2. Enter your username and password (the ones you set in .env)
3. Click **Sign In**

You are now on the main menu. The app remembers your login for 8 hours.

If multiple team members need access, add them to .env:
```
UI_USERS=rajiv:password1,analyst:password2
```

---

## 4. Running Enrichment on an Outscraper CSV

1. From the main menu, click **Start New Run**
2. Click **Choose File** and select your Outscraper CSV export
3. Set **CSV Source** to **Outscraper export**
4. Enter a **Project ID** — a short label for this campaign (e.g. `obgyn-texas-may26`)
5. Your name is pre-filled — leave it or change it
6. Click **Upload and Start Enrichment**

The pipeline starts automatically. You are taken to the run status page.

**How long does it take?**
- 10 records: ~2–4 minutes
- 50 records: ~10–20 minutes
- 200 records: ~45–90 minutes

The page refreshes automatically every 5 seconds while the run is in progress.

---

## 5. Viewing Results

When the run shows **complete**, click the run ID to open the results dashboard.

You will see:
- **Header stats**: total records, counts by tier (Bullseye, Needs Verification, Contender, Manual Review, Excluded), and how many are pending review
- **Filter bar**: click a tier button to show only those records
- **Record table**: practice name, specialty, location, tier, score, QC status

Click any row to expand it and see:
- All signals (yes / no / not found, with evidence text and source links)
- Sales angles generated by Claude
- Exclusion rationale (if the record was excluded)
- Scores (Bullseye, Fit, Confidence)
- The review panel (see below)

---

## 6. Reviewing Records

Each expanded record has a review panel on the right side.

### Add an analyst note
Type your observation in the **Analyst Note** box. It saves when you click **Save**.

### Override the tier
If you disagree with the pipeline's tier:
1. Select a new tier from the **Override Tier** dropdown
2. A required **Override Reason** box appears — explain why
3. Click **Save**

The displayed tier in the table updates immediately.

### Approve or reject
Click **Approve** or **Reject** to mark a record's QC status.
Click **Reset** to set it back to pending.
Click **Save** to persist your choice.

### Your edits are always safe
- The pipeline's original output (`enriched_targets.json`) is **never modified**
- All your notes, overrides, and QC decisions are stored separately in `reviews.json`
- If you refresh the page, all your edits are still there

---

## 7. Exporting

From any completed run's results page:
- **Download JSON**: full enriched output with all pipeline data (for import into other tools)
- **Download CSV**: flat spreadsheet version

Both files contain the original pipeline output only. Review edits (notes, overrides) are stored in `reviews.json` in the run folder on disk. The **Download Client Package** ZIP already incorporates your review overlay — it ships the approved Bullseye/Contender CSVs and reports built from the immutable output plus your overrides.

---

## 8. Where Files Are Stored

All run output is in the folder you set as `OUTPUT_RUNS_PATH`. Each run creates a subfolder:

```
output/runs/
  RUN-20260527-143000/
    input.csv                      ← the CSV you uploaded
    project_config_snapshot.json   ← frozen project config this run used
    icp_snapshot.json              ← frozen ICP profile this run used
    status.json                    ← run status and counts
    run_log.json                   ← pipeline log with per-record error details
    enriched_targets.json          ← enriched records
    enriched_targets.csv           ← same data, flat CSV format
    step4_checkpoint.ndjson        ← per-record checkpoint (lets a failed run resume)
    evidence/<record_id>/          ← Evidence Vault: the page text the crawler saw
    reviews.json                   ← your analyst notes and overrides (safe to open/read)
    refresh_status.json            ← per-record re-enrich job state (spinner data)
    published_briefs.json          ← links for briefs you published (if any)
    link_check_report.json         ← evidence link check results (if you ran one)
    pipeline_stdout.log            ← pipeline output kept when a run fails
```

---

## 9. If Something Goes Wrong

### The run shows "failed"
1. Click the run ID to open its status page
2. The error summary shows the first part of the error message
3. Navigate to the run folder and open `status.json` — the `error_summary` field has the full message
4. If `run_log.json` exists, open it — the `errors` array lists which records failed and why

### The page won't load after starting the server
- Make sure the `pipeline-api` window is still open and shows "Application startup complete"
- Check that your `.env` file exists and has all required values filled in
- Try `http://localhost:8000/login` — if that loads, the server is working

### I forgot my password
- Open `.env`, update `UI_PASSWORD` (or `UI_USERS`) to a new value
- Restart the server (Ctrl+C, then run the uvicorn command again)

### Results are empty after a completed run
- The run completed but no records were enriched
- Open `run_log.json` and check `records_output` and the `errors` array
- Check that `ANTHROPIC_API_KEY` and `OPENAI_API_KEY` are set correctly in the enrichment `.env`

---

## 10. Updating to the Latest Version

When there are code updates:

```
cd C:\users\rajiv\desktop\bemi\enrichment
git pull origin main
cd pipeline-api
python -m pip install -r requirements.txt
```

Then start the server as normal.
